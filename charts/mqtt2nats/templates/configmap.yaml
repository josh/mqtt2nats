apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mqtt2nats.fullname" . }}
  labels:
    {{- include "mqtt2nats.labels" . | nindent 4 }}
data:
  config.json: |
    {{- include "mqtt2nats.configJson" . | nindent 4 }}
